CREATE TABLE Posts(
id TEXT NOT NULL PRIMARY KEY,
title TEXT NOT NULL
);

insertItem:
INSERT OR REPLACE INTO Posts(id, title)VALUES(?,?);

selectAll:
SELECT * FROM Posts;

deleteAll:
DELETE FROM Posts;